import pytest
from solutions import Solution

# ✅ 217. Contains Duplicate
# @pytest.mark.parametrize(
#     'nums, expected',
#     [
#         ([1, 2, 3, 1], True),  # повторяющееся число 1
#         ([1, 2, 3, 4], False),  # все числа уникальны
#         ([1, 1, 1, 3, 3, 4, 3, 2, 4, 2], True),  # много повторов
#         ([], False),  # пустой список
#         ([42], False),  # один элемент
#         ([1, 2, 2], True),  # повтор в конце
#     ]
# )
# def test_contains_duplicate(nums, expected):
#     s = Solution()
#     assert s.containsDuplicate(nums) == expected

########################################################################################################################


